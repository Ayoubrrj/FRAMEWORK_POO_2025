RewriteEngine on

# ROUTES

# --------------------------------------------------
# Catch-all: ignore fichiers statiques, tout le reste → index.php
RewriteCond %{REQUEST_URI} !.(?:css|js|png|jpg|jpeg|gif|svg|ico|woff|woff2|ttf|eot)$ [NC]
RewriteRule ^(.*)$ index.php [L]